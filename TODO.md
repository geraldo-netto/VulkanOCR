# VulkanOCR review — 2026-08-21

| id | severity | effort | description |
| --- | --- | --- | --- |
| VOCR-0057 | low | xs | **README's module map omits proof.py.** README.md:35-43 lists every src module and describes cli.py as carrying the sampling that VOCR-0053 moved out; `proof.py` (sysfs busy sampling as a context manager) is absent. The map says "the claim is the shape, not a census" — and the shape changed. docs/engine-notes.md was updated in the same commit; the README was missed. |
| VOCR-0058 | low | xs | **`logits_for` is duplicated verbatim and aliases the seam it predates.** benchmarks/batching_one_strip.py:25-27 and benchmarks/batching_narrow_only.py:24-26 carry the identical two-line wrapper (docstring included) around `engine.logits` — the public half that VOCR-0007-era work extracted precisely so these scripts would not wrap it. Delete both and call `engine.logits` directly. |
| VOCR-0059 | low | xs | **Four engine builders skip the close the repo preaches.** The live suite's module fixture (tests/test_live_gpu.py:38-42) returns an engine it never closes, and benchmarks/vulkan_vs_cpu.py, batching_one_strip.py and batching_narrow_only.py build engines with no `with`/`close()` — the ~700 MiB-per-engine pattern `OcrEngine.close`'s docstring was written against, and one multi_gpu, dispatch_vs_compute and phase_timings already avoid. Process exit reclaims it, so this is consistency rather than a leak in practice: yield-and-close in the fixture, `with` in the scripts. |
